__pycache__/
*.pyc
*.pyo
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
skills/
.skill-runs.log

# Local Claude Code session data stays out of the repo, but the checked-in
# project config and its hook scripts must not be swept up with it.
.claude/*
!.claude/settings.json
!.claude/hooks/

# Local virtual environments
.venv/
venv/

# Testing artifacts
.coverage
coverage.xml
htmlcov/
.hypothesis/
.pytest_cache/
.mypy_cache/

# Parity test run history (regenerated by `eval_runner.py --parity`)
evals/history/*.jsonl
